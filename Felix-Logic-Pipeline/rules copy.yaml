# =================================================================
# METADATA & FEATURE EXTRACTION ENGINE
# This defines the data types and how to compute features from the CSV.
# Available in environment: row (the current pandas row), pd, len, any, str
# =================================================================
features:
  - name: "construct_id"
    type: "string"
    source: "str(row['gene_id'])"

  - name: "promoter_len"
    type: "int"
    source: "len(str(row['promoter_seq'])) if pd.notna(row['promoter_seq']) else 0"

  - name: "utr5_len"
    type: "int"
    source: "len(str(row['utr5_seq'])) if pd.notna(row['utr5_seq']) else 0"

  - name: "cds_len"
    type: "int"
    source: "int(row['cds_len']) if pd.notna(row['cds_len']) else 0"

  - name: "term_len"
    type: "int"
    source: "len(str(row['terminator_seq'])) if pd.notna(row['terminator_seq']) else 0"

  - name: "expr_mean"
    type: "real"
    source: "float(row['mean_active_tpm']) if pd.notna(row['mean_active_tpm']) else 0.0"

  # Custom Bio-Feature Computations written as inline Python snippets:
  - name: "gc_content"
    type: "real"
    source: "(str(row['full_tu_seq']).upper().count('G') + str(row['full_tu_seq']).upper().count('C')) / len(str(row['full_tu_seq'])) if pd.notna(row['full_tu_seq']) else 0.0"

  - name: "cds_starts_codon"
    type: "bool"
    source: "str(row['cds_seq']).upper().startswith(('ATG', 'GTG', 'TTG')) if pd.notna(row['cds_seq']) else False"

  - name: "cds_ends_stop"
    type: "bool"
    source: "str(row['cds_seq']).upper().endswith(('TAA', 'TAG', 'TGA')) if pd.notna(row['cds_seq']) else False"

  - name: "has_rbs_motif"
    type: "bool"
    source: "any(motif in str(row['utr5_seq']).upper() for motif in ['AGGAGG', 'GGAGG', 'GAGG', 'AAGG']) if pd.notna(row['utr5_seq']) else False"

# =================================================================
# LOGICAL SMT RULES
# =================================================================
rules:
  - name: "Structural Integrity Checklist"
    type: "simple"
    expr: "And(promoter_len > 0, utr5_len > 0, cds_len > 0, term_len > 0)"

  - name: "Valid Start Codon Check"
    type: "simple"
    expr: "cds_starts_codon == True"

  - name: "Valid Stop Codon Check"
    type: "simple"
    expr: "cds_ends_stop == True"

  - name: "Codon Triplet Divisibility"
    type: "simple"
    expr: "cds_len % 3 == 0"

  - name: "GC Content Limit for High Expressors"
    type: "conditional"
    if: "expr_mean > 50.0"
    then: "gc_content <= 0.60"

  - name: "RBS Motif Requirement for High Expressors"
    type: "conditional"
    if: "expr_mean > 50.0"
    then: "has_rbs_motif == True"